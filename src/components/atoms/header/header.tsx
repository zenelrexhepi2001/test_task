import { Stack, Typography } from '@mui/material'
import React from 'react'

const Header = () => {
  return (
    <Stack sx={{paddingTop: '1rem'}}>
        <Typography variant='h4'>Company Cars</Typography>
    </Stack>
  )
}

export default Header